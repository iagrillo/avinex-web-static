Place the provided branding image here as:

assets/images/avinex_splash.png

The startup splash screen is already wired to use that file automatically.
If the image is not present, the app shows a styled Avinex fallback splash.

The sign-in screen also uses the same file (`assets/images/avinex_splash.png`).
If it is missing, the app falls back to `assets/images/app_icon.png`.
